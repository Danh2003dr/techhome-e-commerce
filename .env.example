# Backend API base URL (no trailing slash). Default: http://localhost:8080/api
# Tin nhắn (REST + Socket.IO) dùng cùng host — bỏ hậu tố /api trong code (xem services/socketUrl.ts).
VITE_API_URL=http://localhost:8080/api

# Tuỳ chọn — hiển thị trên trang Admin › Cài đặt (thương hiệu)
# VITE_SITE_TITLE=TechHome
# VITE_SITE_DESCRIPTION=Cửa hàng công nghệ
